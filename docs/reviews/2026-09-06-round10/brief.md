# Review round 10 (audit P0 wave) — QuietInbox

DO NOT activate workflow modes; READ-ONLY review only. No edits, no state-changing git, no instrumented tests, no devices.

Repository: /Users/iml1s/Documents/mine/quietinbox, branch main, HEAD f64ae7b. Review `git diff 3685469..f64ae7b` (one commit).
Context: GPT-5.5 Pro audited the repo (base 96b0cf9) and the findings were filed as GitHub issues ImL1s/quietinbox #1–#17 (bodies also under the same numbers in the issue tracker; the summary is in CHANGELOG.md "[Unreleased]"). This commit claims to fix the five P0s (#1 #2 #3 #4 #5), #7, and the transactional part of #6:

- #1 `CaptureCoordinator`: `admitted()` fence evaluated before waiting for `pipelineMutex` and again inside it; `commitFenced()` at the start of `processJournaled` and right before `ingest.commit`; source add/enable/pause/remove go through the coordinator (`changeSourcePolicy` → vault write + `loadSourcePolicy()` under the lock); the `observeSources` collector only triggers a reload under the lock; disabling/removing a source discards pending journal rows (`JournalDao.discardPending`, new `packageName` column); `replayJournal` runs inside `VaultMaintenance.work`, stops while paused, is triggered on resume / vault ready / maintenance end / source unpause, and marks rows of disabled sources `DISCARDED`; paused sources leave rows PENDING.
- #2 `SettingsRepository`: `mediaCopyEnabled` default false and effective = switch && disclosure; `MediaCopier.copyPending` re-reads it and downgrades PENDING rows to DISABLED_BY_USER when off.
- #3 `VaultMaintenance` (platform/storage/db): `pipelineMutex`, `active`, `work {}` (refused when active, cancellable, structured), `exclusive {}` (flag → cancel+join → lock → block). `VaultRepository.deleteEverything(): ResetResult` verifies each step; `DatabaseHolder.closeAndDeleteFiles(): Boolean`, `MediaDirectory.deleteAll(): Boolean`; `KeyMaterial.epoch` bumped by `destroyAll()`, `BlobCipher` caches `(epoch, aead)`; coordinator `onMaintenance()` rotates the generation, ends/starts sessions, records a `MAINTENANCE` gap (new `GapReason` + strings + label); `SettingsViewModel` surfaces `resetFailedStep` via snackbar.
- #4 `JournalDao.setState` clears payload on non-PENDING; `ConversationDao.rebuildProjection(ids, now)`; `InboxRepository.deleteMessages/deleteConversation` delete media rows in the transaction and files after; `SourceRepository.remove` clears the deletion graph (no LIKE: `substr` prefix match); `RetentionService` under `work {}`, rebuilds projections, no battery constraint; `MediaDao.orphans()` also catches unlinked blobs.
- #5 `KeystoreWrapper.getOrCreateKey()` under a process-wide lock.
- #7 expiry filters (`expiresAtEpochMs IS NULL OR > :now`) in `observeForConversation`, counts, `statsBetween`, `earliestSortKey`, `search`.
- #6 partial: `MediaCopier.store()` writes files → one transaction (blob insert + `setMedia`) → deletes written files on any failure; thumbnail failure ⇒ null; bitmap counted until the copier finishes (`queuedBitmaps` decremented in the media coroutine's finally).
- Schema v3 (`MIGRATION_2_3`, three nullable ADD COLUMN; `schemas/.../3.json`).
- Tests: `CaptureCoordinatorTest` 16 (5 new), `VaultMaintenanceTest` 4 (new module test source set + CI), instrumented `DeletionGraphTest` 5, `MigrationTest` 2→3, `KeystoreWrapperTest`. Claimed: 174 JVM green, storage 13 / crypto 2 instrumented green on the API 36 AVD, negative control (in-lock fence removed ⇒ 3 tests fail).

Verify each claim against the diff. Then hunt for regressions and gaps, in particular: deadlocks or lock-order problems between `pipelineMutex`, `VaultMaintenance.exclusiveMutex`, `DatabaseHolder.mutex` and Room transactions (e.g. `changeSourcePolicy` from a ViewModel while `process()` holds the lock and waits on the DB; `replayJournal` inside `work {}` taking the lock per event while `exclusive` waits); the race window between `_active.value = true` and the coordinator's async `onMaintenance` (events queued in between, `sourcesLoaded=false` reset, the second `admitted()` check); `work {}` registration race (register vs snapshot); `VaultMaintenance.work` swallowing the caller's own cancellation semantics; whether `replayJournal`'s `progressed` loop can spin or starve; whether a PENDING row of a *paused* source blocks the first 200-row page forever; `rebuildProjection` semantics vs the commit path (`lastActivityEpochMs`, preview of 200 chars, ambiguous counts); `substr` prefix match correctness for `scopeKey` (`pkg|profile#identity`); the effective `mediaCopyEnabled` mapping hiding `media_copy=true` from the UI toggle; `BlobCipher` epoch check when `destroyAll()` races `primitive()`; `KeystoreWrapper` lock scope (companion) vs `WrappedSecretFile @Synchronized`; `MediaCopier.store()` cleanup on `CancellationException` after the transaction committed; expiry `now` pinned at Flow collection (documented); string parity en/zh-Hant; docs (CHANGELOG, ARCHITECTURE en/zh, TEST_MATRIX en/zh, SCOPE) matching the code and counts; anything tautological or timing-flaky in the new tests (real `Dispatchers.Default`, `delay(200)` before releasing a gate, Mutex fairness assumption). You may run `./gradlew :platform:capture:testDebugUnitTest :platform:storage:testDebugUnitTest --console=plain` (JVM only; ANDROID_HOME=$HOME/Library/Android/sdk); do not run instrumented tests.

## Output format (繁體中文)
- Verdict: APPROVE | APPROVE WITH MINOR FIXES | REQUEST CHANGES
- Claim verification table (issue → fixed? evidence with file:line); new findings (Critical / Important / Minor) each with a concrete failure scenario and file:line; other observations
