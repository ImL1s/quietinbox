# Review round 16 (mini re-review of the round-15 fixes) — QuietInbox

DO NOT activate workflow modes; READ-ONLY review only. No edits, no state-changing git, no instrumented tests, no devices.

Repository: /Users/iml1s/Documents/mine/quietinbox, branch main. Review `git diff 6225719..a9609e0` (one commit, `a9609e0`). Round-15 reports: `docs/reviews/2026-09-06-round15/{gemini-3.8-flash-high-agy,claude-subagent}.md` — the subagent's three Minors (all in `releaseHeld()`'s stale-key suppression) are what this commit fixes. Only `CaptureCoordinator.kt` (`releaseHeld`, `Held.postId`) and `CaptureCoordinatorTest.kt` changed in code.

Claims to verify (each with file:line):
- Minor-1 (mid-release disconnect): `releaseHeld()` reads `activeGeneration` and `paused` once (`liveGeneration`, `livePaused`) and classifies every item of the batch against those; stale items are collected and decided after the loop. New test "a disconnect landing while held notifications are released does not let a later one suppress itself" (`factory.create` for the first item calls `onDisconnected()`; the second is still snapshotted and queued, both fenced by the consumer → `droppedAfterRevoke == 2`, no COLD_START gap, `create` exactly 2).
- Minor-2 (same key, newer post): suppression is keyed by `Held.postId = sbn.key + "|" + sbn.postTime`. New test "a stale copy with the same key but an older post time is a loss of its own" (exactly 1 COLD_START gap).
- Minor-3 (keys of never-enabled apps): the suppression set `queuedPosts` is filled only from items actually `enqueue`d (so capturable sources only); a stale item is only compared when it is capturable itself.
- Tests: `sbnOf(pkg, id, key, postTime)` is the single notification mock; both 300-item loops use it. Claimed: 32 capture tests green three runs in a row, each new test failing when its guard is removed (controls: post time removed from `postId`; per-item re-read of `activeGeneration`/`paused`; stale check widened to `enabledPackages` only and the queued-post check removed), 206 JVM tests green, lint 0 errors. Docs: CHANGELOG Unreleased, TEST_MATRIX en/zh (32 / 206), CLAUDE.md audit trail (rounds 10–15), reviews index rows 14–15.

Hunt for regressions in exactly these changes: a stale item whose live twin's `snapshotFactory.create` failed (not queued → not in `queuedPosts` → gap recorded — correct?); items with `captured != null` (synthetic / pre-captured, `sbn == null`, `postId == null`) — `null in queuedPosts` is false, so a stale pre-captured item of a capturable source records a gap — acceptable?; the `dropped == 0` guard still skips the stale gap when an overflow gap was recorded — the overflow gap starts at the first eviction (before every survivor), confirm the coverage argument; `liveGeneration` read once — an item queued under a generation that dies right after is fenced by `admitted()` and counted in `droppedAfterRevoke` with no gap (existing convention for queued events; say whether you accept it); reading `sbn.postTime` (framework metadata, `SnapshotFactory` already reads it) — acceptable?; test determinism of the two new tests (the mid-release disconnect is triggered synchronously inside the `factory.create` answer on the releasing thread; `awaitUntil { droppedAfterRevoke == 2 }`). Docs vs code counts (32, 206). You may run `./gradlew :platform:capture:testDebugUnitTest --console=plain -q` (JVM only; ANDROID_HOME=$HOME/Library/Android/sdk); do not run instrumented tests.

## Output format (繁體中文)
- Verdict: APPROVE | APPROVE WITH MINOR FIXES | REQUEST CHANGES
- Round-15 verification table (finding → fixed? evidence with file:line); new findings (Critical / Important / Minor) each with a concrete failure scenario and file:line; other observations
