# Review round 15 (mini re-review of the round-14 fixes) — QuietInbox

DO NOT activate workflow modes; READ-ONLY review only. No edits, no state-changing git, no instrumented tests, no devices.

Repository: /Users/iml1s/Documents/mine/quietinbox, branch main. Review `git diff eae0003..6225719` (one commit, `6225719`). Round-14 reports: `docs/reviews/2026-09-06-round14/{gemini-3.8-flash-high-agy,claude-subagent}.md` — the subagent's five Minors (and its observations 4 and 5) are what this commit addresses.

Claims to verify (each with file:line):
- Minor-1: `releaseHeld()` writes the overflow gap and the stale-held gap through `recordColdStartLoss(start)`, which runs `recordGap` under `guarded {}` and, when it fails, sets `coldStartLossSince = start ?: now` if it was null, so the next `settleColdStartGap()` writes it. New test "an overflow gap whose write failed is kept and written on the next policy load" (300 notifications, `recordGap` throws once then succeeds, exactly 1 then exactly 2, a third policy load adds none).
- Minor-2: `loadSourcePolicy()` after `sourcesLoaded = true` (and the held re-release) reads `coldStartGapId`; if non-null it `closeOpenGaps(COLD_START)` and nulls it. `dropHeld()` writes `coldStartGapId` (inside `openGap`'s assignment) then reads `sourcesLoaded`; both fields are `@Volatile`. New test "a cold-start gap row that lands between the policy's settle and the flag flip is closed by the policy load" (the settle's `closeOpenGaps` is gated open so the row lands before the flip; atLeast 2 closes). The earlier "closed at once" test now asserts `atLeast = 2`.
- Minor-3: the stale gap counts only `pkg in enabledPackages && pkg !in pausedPackages` and skips a stale item whose `sbn?.key` is also held under the current generation (`liveKeys`, built from `sbn?.key` — framework metadata, not content). `sbnOf` in the test now gives each mock a distinct key. New test "a notification held across a disconnect gets no gap when it is paused or captured again by the resync" (the second copy is offered synchronously via `onPosted`, because the real resync runs on its own coroutine).
- Minor-4: `platform/storage/src/androidTest/.../MediaExportBoundTest.kt` (1 test): `maxId()` is 0 on an empty table and the empty page ends the loop; after two blobs, `exportPage(0, firstId, 10, now)` returns only the first, `exportPage(firstId, firstId, …)` is empty, `exportPage(0, secondId, …)` returns both.
- Minor-5: the bitmap-bound test asserts `bitmaps.count { it == null } shouldBe 1` after `size shouldBe 9`.
- Observations: `coldStartTimeoutMs` annotated `@VisibleForTesting(otherwise = PRIVATE)`; `docs/SCOPE.md` cold-start line lists the stale-held source and "forgotten only once that write succeeded".
- Claimed verification: 204 JVM tests green (capture 30, four consecutive runs green), each of the three new tests fails when its guard is removed (controls D/E/F), lint 0 errors, instrumented storage 16 / crypto 2 / backup 2 green on the API 36 AVD. Docs: CHANGELOG Unreleased, TEST_MATRIX en/zh (30 / 204 / storage 16), SCOPE, CLAUDE.md audit trail (rounds 10–14), reviews index rows 13–14.

Hunt for regressions in exactly these changes: `recordColdStartLoss` runs on `scope` outside the pipeline lock — can its `coldStartLossSince = …` race `settleColdStartGap()`'s `if (written && since != null) coldStartLossSince = null` (lost update: settle reads null, the launched write fails and sets t, settle does nothing → kept, fine; or settle reads t0, writes, clears — meanwhile the launched write fails and does not set because non-null → lost?). Is `liveKeys` correct when `paused` is true (everything is stale, `liveKeys` empty, `capturable` requires not paused per-source only — should a global pause suppress the stale gap?). Does reading `sbn.key` break the "read nothing before the policy is known" rule (key is framework metadata like packageName — say whether you accept that). The re-check after the flag flip runs `closeOpenGaps` under the pipeline lock on every policy load when `coldStartGapId` is non-null — only then, so cost is nil; confirm. `coldStartGapId = null` in `loadSourcePolicy` vs `dropHeld`'s own null after its close — idempotent? Test determinism for the three new tests (real dispatchers; latches vs timing). Docs vs code counts (30, 204, 16). You may run `./gradlew :platform:capture:testDebugUnitTest --console=plain -q` (JVM only; ANDROID_HOME=$HOME/Library/Android/sdk); do not run instrumented tests.

## Output format (繁體中文)
- Verdict: APPROVE | APPROVE WITH MINOR FIXES | REQUEST CHANGES
- Round-14 verification table (finding → fixed? evidence with file:line); new findings (Critical / Important / Minor) each with a concrete failure scenario and file:line; other observations
