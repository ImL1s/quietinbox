# Review round 11 (audit wave 2 + round-10 fixes) — QuietInbox

DO NOT activate workflow modes; READ-ONLY review only. No edits, no state-changing git, no instrumented tests, no devices.

Repository: /Users/iml1s/Documents/mine/quietinbox-wave2 (a git worktree of ImL1s/quietinbox on branch `wave2`), HEAD 825d708. Review `git diff f64ae7b..825d708` (one commit on top of the round-10 base f64ae7b).
Round-10 reports: `docs/reviews/2026-09-06-round10/{gemini-3.8-flash-high-agy,claude-subagent}.md`. This commit claims to fix every round-10 Important and the actionable Minors, and to close GitHub issues #6 #8 #9 #10 #11 #12 #13 #14 #15 #16 (bodies: `gh issue view <n> --repo ImL1s/quietinbox`; summary in CHANGELOG.md "[Unreleased]"):

Round-10 fixes: replay excludes `pausedPackages` at the query (`JournalDao.pendingExcluding`, `IngestRepository.pendingJournal(limit, excludingPackages)`); `BlobCipher.build()` returns `Stale` when the epoch moved and `primitive()` retries once then fails closed; `VaultMaintenance.addListener` + `MaintenanceListener` called synchronously in `exclusive` (start before cancel/join, end in `finally`), the coordinator registers one and no longer collects `active`; `VaultRepository.deleteEverything` has `finally { if (!Ready) holder.retry() }` and `VaultRepositoryTest` (3, mocked holder); `onMaintenance(false)` launches its DB bookkeeping and replay instead of awaiting; `MediaCopier.store()` clears its cleanup list inside the transaction; `IngestRepository` previews cut at 200 code points (`takeCodePoints`); reset failure snackbar maps step → localised string; the lock-wait test uses `CoroutineStart.UNDISPATCHED` instead of `delay(200)`; `DeletionGraphTest` no-op line removed.
#13 `CaptureCoordinator`: `Held` buffer (`hold`/`coldStart`/`releaseHeld`/`dropHeld`, `MAX_HELD=64`, `COLD_START_TIMEOUT_MS=15_000`, `GapReason.COLD_START`), `offer()` holds the framework object before `sourcesLoaded`, `loadSourcePolicy()` releases held items, `snapshotFactory` is an internal var for tests.
#9 `Reconciler.aligns()` (ids when both sides have one, else fingerprint) used by `suffixPrefixOverlap`/`containedAt` with a match predicate; `SuppressionRule` + `SuppressionDao.token`, used in `IngestRepository.commit`; tokens store `sourceMessageId`/`postedAtEpochMs` (written in wave 1).
#11 `SearchDao.searchCandidates` keyset (sortKey, id) + `SearchRepository.searchPage` loop (`CANDIDATE_PAGE=200`, `MAX_CANDIDATE_PAGES=200`); `ObservedMessage.senderKey`/`senderIdentity`, per-conversation `medianIntervalMs`, `intervalSampleSize`, `SenderRank`/`SenderPhrases` carry package + conversation.
#10 `SearchViewModel`/`ConversationViewModel` observe `VaultRepository.state` (`vaultLocked`, `vaultOpening`/`loading`), screens show `EmptyState` + retry; tests with `Dispatchers.setMain(Dispatchers.Unconfined)`.
#15 `ConversationDao.unviewedCount`, `InboxRepository.unviewedConversationCount`, `ReminderPolicy.shouldRemind`, worker posts only with unviewed > 0 (plural string), `rescheduleNow()` awaited, permission check in `post()`.
#14 manifest `disabled_filter_types` removed; `ListenerAccess.settingsIntents()/openSettings()` fallback chain; three screens show `listener_settings_manual` when nothing opens.
#16 `BackupService`: `export` = `maintenance.work`, `import` = `maintenance.exclusive`; `writeRecords` streams keyset pages (`exportPage`/`exportCount` per DAO, expiry-filtered) inside one `withTransaction`; `BackupResult.Ok(counts, skippedMedia)` + `backup_result_partial_media`; restore uses `rebuildProjection`; `BackupRoundTripTest` (instrumented, backup module) + CI lane.
#12 `distributionSha256Sum`, `abortOnError = true` (app + library convention), issue forms, PR template, CODEOWNERS, Dependabot (actions only). #8/#17 docs (COMPATIBILITY en/zh, SCOPE), inbox `Work` icon for `profileKey != "user:0"`. About string fixed.
Claimed: 193 JVM tests green, lint 0 errors, release APK without INTERNET, instrumented storage 15 / crypto 2 / backup 1 green on the API 36 AVD.

Verify each claim against the diff. Then hunt: the `Held` buffer (memory of `StatusBarNotification` references, generation staleness, `releaseHeld` running under the pipeline lock, `coldStartJob` re-launch races, onboarding resync now buffered, `hold` when paused); `aligns()` semantics against the six §7.2 examples and the property tests; `SuppressionRule` decision table and the `token` query vs the old `isSuppressed`; keyset search: duplicate/missed rows at equal `sortKey`, the `exhausted` logic when the last page is exactly `pageSize`, `MAX_CANDIDATE_PAGES` returning a cursor; `import` under `exclusive` while the settings screen's own coroutine is not a worker (self-deadlock?), `export` under `work` with a long transaction blocking capture commits; `rebuildProjection(convMap.values, now)` vs restored `lastActivityEpochMs`; `MaintenanceListener` callbacks throwing or suspending inside `exclusive`; `finally { holder.retry() }` when `closeAndDeleteFiles` itself threw; `deleteEverything` `return@exclusive` inside try/finally; `ReminderWorker` behaviour when the vault is locked; `openSettings` with `resolveActivity` on API 30+ package-visibility (queries) restrictions — does `resolveActivity` return null for Settings on some devices?; string parity en/zh-Hant (plurals included); docs (CHANGELOG, ARCHITECTURE en/zh, TEST_MATRIX en/zh, SCOPE, COMPATIBILITY en/zh, reviews index) matching code and counts; anything tautological or timing-flaky in the new tests. You may run `./gradlew test --console=plain` (JVM only; ANDROID_HOME=$HOME/Library/Android/sdk) in the worktree; do not run instrumented tests.

## Output format (繁體中文)
- Verdict: APPROVE | APPROVE WITH MINOR FIXES | REQUEST CHANGES
- Round-10 verification table (finding → fixed? evidence with file:line); issue claim table (#6 #8 #9 #10 #11 #12 #13 #14 #15 #16 → done? evidence); new findings (Critical / Important / Minor) each with a concrete failure scenario and file:line; other observations
