# Review round 13 (mini re-review of the post-round-12 commits) — QuietInbox

DO NOT activate workflow modes; READ-ONLY review only. No edits, no state-changing git, no instrumented tests, no devices.

Repository: /Users/iml1s/Documents/mine/quietinbox, branch main. Review `git diff a3fd01b..c6b6645` (commits d409d4b "round-12 review findings", c8e4c9d "release 0.1.1", c6b6645 "CI-caught ordering"). These shipped as GitHub release v0.1.1 (tag at c6b6645). Round-12 reports: `docs/reviews/2026-09-06-round12/{gemini-3.8-flash-high-agy,claude-subagent}.md`.

Claims to verify:
- Round-12 subagent Minor-1..8 and "other observation 1/3/4" plus agy Minor-1/2: `coldStartLossSince` / `vaultGapSince` remembered when `openGap` fails under a locked vault and written as bounded gaps when the vault opens (`settleColdStartGap()` in `loadSourcePolicy`, the vault-Ready collector); `heldDroppedSince` as the gap start; `coldStart()` re-checks the buffer; `coldStartGapId = null` on maintenance start with `closeOpenGaps(COLD_START)` now unconditional; `BackupService` media paged outside the transaction with an honest KDoc; `SuppressionRuleTest` covers differing ids with a missing post time; `ListenerAccess.settingsIntent` + three ViewModel wrappers removed; `unviewedCount` requires `messageCount > 0`; unexpected reset step localised; `relativeTime` reads the locale via the composition; synthetic publisher checks `POST_NOTIFICATIONS` next to `notify()` and the capture library manifest declares it; CHANGELOG/SCOPE/TEST_MATRIX/reviews index updated.
- c6b6645: `loadSourcePolicy()` now calls `releaseHeld()` *before* `sourcesLoaded = true` and re-checks the buffer after; the bitmap-bound test keeps every copy in flight until released. CI had journaled `[evt-ok, evt-busy]` (a later arrival overtook the held one) before this change.
- Claimed: 198 JVM tests green (capture suite passed three consecutive runs), lint 0 errors in every module, instrumented storage 15 / crypto 2 / backup 2 green, CI on main green for c6b6645 (JVM, assemble + permission gate, API 29, API 35), release APK: sha matches SHA256SUMS, signer = upload key, versionCode 5, no INTERNET.

Hunt for regressions in exactly these changes: the new ordering in `loadSourcePolicy()` (an item held while `sourcesLoaded` is still false but after `releaseHeld()` took its snapshot — is it released by the trailing check or by the cold-start job, and can it be released twice?); `settleColdStartGap()` running under the pipeline lock while the vault is locked (throws inside `guarded`, fine?) and the `coldStartLossSince ?: now` fallback; `vaultGapSince` set from `snapshot.observedAtEpochMs` while `vaultGapOpen` is only reset on Ready; media paging outside the transaction (a page read while retention deletes rows — any exception path that leaves the stream without an `End` record?); the removed wrappers (any remaining caller?); string parity en/zh-Hant; docs vs code counts (198 JVM, capture 24, SuppressionRule 4, backup instrumented 2). You may run `./gradlew test --console=plain -q` (JVM only; ANDROID_HOME=$HOME/Library/Android/sdk); do not run instrumented tests.

## Output format (繁體中文)
- Verdict: APPROVE | APPROVE WITH MINOR FIXES | REQUEST CHANGES
- Round-12 verification table (finding → fixed? evidence with file:line); new findings (Critical / Important / Minor) each with a concrete failure scenario and file:line; other observations
