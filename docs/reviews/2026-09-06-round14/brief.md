# Review round 14 (mini re-review of the round-13 fixes) — QuietInbox

DO NOT activate workflow modes; READ-ONLY review only. No edits, no state-changing git, no instrumented tests, no devices.

Repository: /Users/iml1s/Documents/mine/quietinbox, branch main. Review `git diff c6b6645..eae0003` (one commit, `eae0003`, on top of the released v0.1.1 = `c6b6645`; `537ad80` in between is a docs-only SCOPE row). Round-13 reports: `docs/reviews/2026-09-06-round13/{gemini-3.8-flash-high-agy,claude-subagent}.md` — the subagent's five Minors are what this commit fixes.

Claims to verify (each with file:line):
- Minor-1: `settleColdStartGap()` clears `coldStartLossSince` only after its `guarded {}` write succeeded (`written && since != null`); the vault-Ready collector clears `vaultGapOpen` / `vaultGapSince` only after its write succeeded. New test "a cold-start loss whose settle failed is kept and written on the next policy load" (`recordGap` throws once, then succeeds; exactly 2 calls; a third policy load adds none).
- Minor-2: `dropHeld()` — when `openGap` returns and `sourcesLoaded` is already true, the row is closed at once (`closeOpenGaps(COLD_START)`, `coldStartGapId = null`). `coldStartTimeoutMs` is an `internal var` test seam (default `COLD_START_TIMEOUT_MS`). New test "a cold-start gap row that lands after the policy loaded is closed at once, not left open" (timeout 200 ms, gated `openGap`, exactly 2 `closeOpenGaps`).
- Minor-3: `releaseHeld()` — an item skipped for a stale generation / pause whose package is enabled contributes to `staleSince`; one bounded `COLD_START` gap from the earliest such arrival is recorded (only when no overflow gap was already recorded, `dropped == 0`, since that one starts earlier). New test "a notification held across a disconnect is recorded as a gap, not dropped silently".
- Minor-4: `MediaDao.maxId()` (`COALESCE(MAX(id), 0)`) read inside the export transaction; `exportPage(afterId, maxId, limit, now)` adds `AND id <= :maxId`; `BackupService.writeRecords` returns `expected to maxId` from the transaction and pages media with it. KDoc updated.
- Minor-5: reviews index row 12 (en/zh) now says seven Minors fixed and the manifest media count documented.
- Claimed verification: 201 JVM tests green (capture 27), each new test fails when its guard is removed (negative controls run by patching the guard out), lint 0 errors in all modules, instrumented storage 15 / crypto 2 / backup 2 green on the API 36 AVD. Docs: CHANGELOG `[Unreleased]`, TEST_MATRIX en/zh (27 / 201), ADR-0007 en/zh wording, CLAUDE.md audit trail, round-13 reports archived.

Hunt for regressions in exactly these changes: `vaultGapOpen` staying true after a failed close — does `process()` still record a *new* lock-out correctly (it skips `openGap` while `vaultGapOpen`)? Any path where `coldStartLossSince` is now never cleared (e.g. `since != null` but `written` stays false forever)? `dropHeld()` runs outside the pipeline lock: the new `closeOpenGaps` after the insert vs a concurrent `settleColdStartGap()` — can a row be left open or closed twice (idempotent?) and can `coldStartGapId = null` race a later `dropHeld` (`if (coldStartGapId != null) return`)? The stale-gap in `releaseHeld()`: `paused` items vs `activeGeneration == null`, overlapping gaps with the pause / disconnect gap, the `dropped == 0` guard. `maxId` with `COALESCE(MAX(id), 0)` on an empty table (no media exported, loop ends on the first empty page?). Test determinism (real dispatchers; `coVerify(timeout = …)` / `stillHolds` patterns) and whether each negative control is genuine. Docs vs code counts (27, 201). You may run `./gradlew :platform:capture:testDebugUnitTest --console=plain -q` (JVM only; ANDROID_HOME=$HOME/Library/Android/sdk); do not run instrumented tests.

## Output format (繁體中文)
- Verdict: APPROVE | APPROVE WITH MINOR FIXES | REQUEST CHANGES
- Round-13 verification table (finding → fixed? evidence with file:line); new findings (Critical / Important / Minor) each with a concrete failure scenario and file:line; other observations
