# Review round 18 (localisation: zh-Hans, ja, ko) — QuietInbox

DO NOT activate workflow modes; READ-ONLY review only. No edits, no state-changing git, no instrumented tests, no devices.

Repository: /Users/iml1s/Documents/mine/quietinbox, branch main. Review `git diff 69a60b4..7ef07de` (one commit, `7ef07de`): the app gains Simplified Chinese (`values-b+zh+Hans`), Japanese (`values-ja`) and Korean (`values-ko`) catalogues for `core/designsystem` (strings.xml 318 strings + 1 plurals, plurals.xml 2 plurals, strings_analytics.xml 60 strings) and `platform/capture` (1 string); `localeFilters` and a new `app/src/main/res/xml/locales_config.xml` (+ `android:localeConfig` in the manifest); `tools/check-strings.py` (parity gate over every resource XML of each values directory: names, `%n$s/%n$d/%%` placeholders, plurals `other`), wired into the CI "Assemble + permission gate" job; Google Play listings for zh-CN / ja-JP / ko-KR under `fastlane/metadata/android/` (title, short/full description, changelogs 4 and 5, images: phone screenshots from the demo vault, shared feature graphic and icon) and `fastlane/whatsnew/whatsnew-{zh-CN,ja-JP,ko-KR}`; `tools/demo-screenshots.sh` accepts the three new locales (tab labels, onboarding buttons per language); docs: CHANGELOG `[Unreleased] Added`, SCOPE row, TEST_MATRIX row (en/zh), CLAUDE.md strings rule, RELEASE.md (Play uploads only what's-new; a new listing language is added once in the Play Console), README module line, and a new `docs/zh-Hant/SCOPE.md` (full Traditional Chinese translation of `docs/SCOPE.md`, cross-linked).

You are the translation quality reviewer as much as the code reviewer. Please check, with concrete examples (name → text → problem → suggested text):
1. **Translation fidelity and naturalness** in each of the three languages: mistranslations, wrong register (the app speaks to the user as 你 / 敬体 / 해요체-합니다체 consistently?), terms that must stay consistent across the catalogue (vault = 保险库/数据库 vs 保管庫 vs 보관소; source = 来源 / ソース / 소스; capture = 捕获 / キャプチャ / 캡처; gap = 中断 / 欠落 / 누락; recovery key = 恢复密钥 / 復旧キー / 복구 키; notification access = 通知使用权 / 通知へのアクセス / 알림 액세스), Android platform vocabulary that should match the OS's own strings (e.g. Android zh-CN "通知使用权", "受限制的设置", "应用信息", "工作资料"; ja "通知へのアクセス", "制限付き設定", "アプリ情報", "仕事用プロファイル"; ko "알림 액세스", "제한된 설정", "앱 정보", "직장 프로필"), and any claim that drifted from the English meaning (the strings are privacy promises; a translation must not promise more or less than the English).
2. **Format safety**: placeholders preserved (`%1$s`, `%2$d`, `%%`), XML escaping (apostrophes, `&amp;`), `\n` in `about_limitations_body`, the Korean particle handling `%1$s을(를)`, the `ob_step_of` argument order in Korean (`%2$d단계 중 %1$d단계`).
3. **Length / layout risk** for bottom-navigation labels and buttons (the Japanese tab label was already shortened from アクティビティ to 活動 after it wrapped on the AVD; are any other labels likely to wrap at 360 dp?).
4. **Store texts**: Play limits (title ≤ 30, short ≤ 80, full ≤ 4000, changelog ≤ 500 chars), consistency with the app strings, and that the zh-CN text uses mainland vocabulary rather than converted Taiwan phrasing.
5. **Code / tooling**: `tools/check-strings.py` correctness (does it really fail on a missing string, a changed placeholder, a plurals without `other`? try mentally or with a scratch copy — no edits in the repo), `locales_config.xml` vs `localeFilters` vs the actual APK locale list (`--_-- ja ko zh-Hans zh-Hant`), and whether anything in the code assumes only two locales (search `values-b+zh+Hant`, `zh-TW`, `Locale` handling in `Formatting.kt`, the relative-time / weekday helpers, `DemoDataRepository` locale-dependent content, the reminder plurals via `getQuantityString`).
6. **Docs vs code**: `docs/zh-Hant/SCOPE.md` faithful to `docs/SCOPE.md` (same rows, same claims, nothing added); the CHANGELOG line; TEST_MATRIX row; CLAUDE.md rule.
Claimed verification: `python3 tools/check-strings.py` OK (0 errors); `./gradlew :app:assembleDebug :app:assembleRelease lint` green, lint 0 errors in every module (Android lint's `MissingTranslation` is error-level); release APK still has no INTERNET permission; APK locales `ja ko zh-Hans zh-Hant` + default; `tools/demo-screenshots.sh` walked onboarding, tabs and search on the API 36 AVD in all three languages and produced the store screenshots; the screenshots were inspected (only the Japanese tab wrap was found and fixed).

You may run `python3 tools/check-strings.py` and `./gradlew :core:designsystem:lintDebug --console=plain -q` (ANDROID_HOME=$HOME/Library/Android/sdk); do not run instrumented tests or touch devices.

## Output format (繁體中文)
- Verdict: APPROVE | APPROVE WITH MINOR FIXES | REQUEST CHANGES
- Per language: a table of concrete translation issues (name → current → problem → suggested), severity Critical (meaning changed / promise broken / placeholder broken) · Important (wrong term, unnatural, OS vocabulary mismatch) · Minor (style); then code/tooling findings; then docs findings; other observations
