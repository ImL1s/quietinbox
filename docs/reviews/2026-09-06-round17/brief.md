# Review round 17 (mini re-review of the round-16 fixes) — QuietInbox

DO NOT activate workflow modes; READ-ONLY review only. No edits, no state-changing git, no instrumented tests, no devices.

Repository: /Users/iml1s/Documents/mine/quietinbox, branch main. Review `git diff a9609e0..69a60b4` (one commit, `69a60b4`). Round-16 reports: `docs/reviews/2026-09-06-round16/{gemini-3.8-flash-high-agy,claude-subagent}.md` — the subagent's two (non-blocking) Minors and observations 1–3 are what this commit addresses. Code changed only in `CaptureCoordinator.kt` (`releaseHeld`, `enqueue` returns Boolean) and `CaptureCoordinatorTest.kt`.

Claims to verify (each with file:line):
- Minor-1: `releaseHeld()` decides each item against `activeGeneration` / `paused` at its own moment (per-item reads again); the suppression set `queuedPosts` is filled only when `enqueue(...)` returned true (bounded queue took the event) — never predicted up front — so an item can never be suppressed by itself, and one that a disconnect or pause overtakes mid-release is stale, not in `queuedPosts`, and gets a bounded `COLD_START` gap via `staleSince`. The test "a disconnect landing while held notifications are released gives the later ones a gap instead of letting them suppress themselves": `factory.create` for the first item calls `onDisconnected()`; expected exactly 1 `create`, `droppedAfterRevoke == 1` (the first, fenced), exactly 1 `recordGap(COLD_START, BOUNDED)` (the second), no journal.
- Minor-2: the cold-start test keeps the unlisted mock and asserts `verify(exactly = 0) { unlisted.key; unlisted.postTime; unlisted.notification }`.
- Observation 3: `enqueue` returns `ok`; a post whose `trySend` failed is not added to `queuedPosts` (its stale copy would then get a gap — the overflow already has an exact QUEUE_OVERFLOW gap; say whether the extra bounded gap is acceptable or double counting).
- Observation 1: `docs/SCOPE.md` cold-start line adds "(unless the reconnect's resync offered the same post again)". Observation 2: index row 15 = `a9609e0`, row 16 added.
- Claimed verification: 32 capture tests green three runs in a row; negative controls: (I) suppression set predicted from all items → the mid-release test and the two other stale-gap tests go red; (J) reading `postId` of every item before the loop → the cold-start verify goes red; 206 JVM tests green; lint 0 errors. Docs: CHANGELOG Unreleased, TEST_MATRIX en/zh (32 / 206), CLAUDE.md audit trail (rounds 10–16), reviews index rows 15–16.

Hunt for regressions in exactly these changes: with per-item reads restored, is round-15's Minor-1 (self-suppression) really impossible now (argue from `queuedPosts` being filled only after `enqueue` of the same item, and a stale item being one that was never enqueued in this loop)? The mid-release test's determinism (`onDisconnected()` runs synchronously inside the `factory.create` answer on the releasing thread, before `enqueue` of the first item — so the first is queued under a dead generation and fenced; the second is read after the flip). `verify(exactly = 0) { unlisted.key; … }` on a relaxed mockk — does mockk record property reads on relaxed mocks so the negative control is real (the lead says control J went red)? Any remaining path where a held notification's loss is neither a gap nor a count? Docs vs code counts (32, 206). You may run `./gradlew :platform:capture:testDebugUnitTest --console=plain -q` (JVM only; ANDROID_HOME=$HOME/Library/Android/sdk); do not run instrumented tests.

## Output format (繁體中文)
- Verdict: APPROVE | APPROVE WITH MINOR FIXES | REQUEST CHANGES
- Round-16 verification table (finding → fixed? evidence with file:line); new findings (Critical / Important / Minor) each with a concrete failure scenario and file:line; other observations
